"""
Code Indexer — deterministic code parsing and indexing.

Downloads repo files, parses them with AST/regex, stores structured index in DB.
Replaces expensive LLM-based file exploration with zero-cost deterministic parsing.
"""
