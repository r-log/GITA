import { helper } from './helper';
import bar from './bar';

const GREETING = 'hello';

function add(a, b) {
  return a + b;
}

function multiply(a, b) {
  return a * b;
}

async function fetchData(url) {
  return null;
}
