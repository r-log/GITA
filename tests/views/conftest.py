"""Fixtures specific to the view tests.

``indexed_synth_py`` was moved to ``tests/conftest.py`` so agent tests can
reuse it too. This file is intentionally minimal — any view-only fixtures
land here in the future.
"""
